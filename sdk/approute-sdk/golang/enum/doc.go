// Package enum defines typed string constants used throughout the AppRoute SDK.
//
// Each enum type is a named string so callers get compile-time safety when
// passing values to the API while still being able to compare against raw
// JSON strings.
package enum
